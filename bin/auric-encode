#!/bin/zsh

(( $#argv > 0 )) || set -- disc-*
typeset disc wav mp3
for disc in $@:t; do
    for wav in $disc/**/master.wav(N); do
        mp3=$wav:h/listen.mp3
        print $'\e[34;1m'$wav:h$'\e[0m'
        [[ -e $mp3 ]] || lame --nohist --preset fast standard $wav $mp3
    done
done
